//Type declaration
// #include<stdio.h>

// int main(){
//     int a,b,c,d;
//     a=b=c=d=30;
//     printf("%d",a);
// return 0;
// }

//Arithimetic Operation
#include<stdio.h>
#include<math.h>

int main(){
    // int a=2,b=3,z;
    // z=b*a;
    // printf("print %d",z);
    // printf("print %d",z%5);
    // printf("print %d",-z%5);

    // there is no operator to perform exponentiation in c
    // printf("%d", 2^1); //it perform bitwise xor operatorion

    // to find exponent value you have to incude math.h library and use pow(a,b) function
    // printf("the  power value of a is to b is %f", pow(2,3)); //You have to use %f since %d is not valid for pow funtion, so a nd be as integer are invalid.
    // float k=3.0/9;
    // printf("the value of %f", k);

return 0;
}


