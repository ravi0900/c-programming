#include<stdio.h>

int main(){
    int length, breath;
    printf("what is the length and breath of rectangele \n");
    scanf("%d", &length);
    scanf("%d", &breath);
    printf("Area of rectangle is %d", length*breath);
return 0;
}